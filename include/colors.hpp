#include <SFML/Graphics/Color.hpp>
#include <vector>

const inline std::vector<sf::Color> toxicColors = {
    sf::Color(191, 255, 0), // Neon green
    sf::Color(255, 0, 255), // Neon pink
    sf::Color(0, 255, 255), // Cyan
    sf::Color(255, 69, 0),  // Neon orange
    sf::Color(255, 0, 0),   // Neon red
    sf::Color(128, 0, 128)  // Neon purple
};
const inline std::vector<sf::Color> catpuccinoColors = {
    sf::Color(185, 160, 130), // Cat fur color
    sf::Color(115, 100, 85),  // Cat stripes color
    sf::Color(255, 255, 255), // Cat eye color
    sf::Color(200, 150, 100), // Cat nose color
    sf::Color(160, 120, 80),  // Cat ear color
    sf::Color(250, 220, 180), // Cat paw color
    sf::Color(150, 120, 100)  // Cat tail color
                              // Add more Catpuccino colors here if needed
};
const inline std::vector<sf::Color> gruvboxColors = {
    sf::Color(251, 241, 199), // Gruvbox light background
    sf::Color(59, 110, 135),  // Gruvbox dark background
    sf::Color(76, 86, 106),   // Gruvbox light text
    sf::Color(169, 169, 169), // Gruvbox dark text
    sf::Color(170, 85, 0),    // Gruvbox orange
    sf::Color(215, 153, 33),  // Gruvbox yellow
    sf::Color(119, 136, 153), // Gruvbox blue
    sf::Color(129, 94, 247),  // Gruvbox purple
    sf::Color(215, 153, 33),  // Gruvbox bright yellow
    sf::Color(130, 170, 255)  // Gruvbox bright blue
};
const inline std::vector<sf::Color> nightfoxColors = {
    sf::Color(85, 98, 112),   // Steel blue
    sf::Color(92, 106, 121),  // Slate gray
    sf::Color(137, 156, 178), // Light steel blue
    sf::Color(176, 196, 222)  // Light sky blue
};
const inline std::vector<sf::Color> gruvboxDarkOrangeColors = {
    sf::Color(128, 128, 128), // Gray
    sf::Color(235, 219, 178), // Light orange
    sf::Color(224, 108, 117), // Red
    sf::Color(163, 134, 77),  // Brown
    sf::Color(215, 153, 33),  // Yellow
    sf::Color(166, 226, 46)   // Green
};
